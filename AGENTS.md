# AGENTS.md —— 唐瑞遥 Candice 个人网站

> 给未来的 Codex / 协作者：这是一个**纯静态、零构建**的个人介绍网站（HTML + CSS + 原生 JS），
> 双击即开，本地用 `python -m http.server` 预览。本文件记录**设计风格、当前进度、待办**。
> 详细设计规范见 `docs/00~03`，本文件是"速查 + 现状"。

---

## 1. 项目概览

- **主人公**：唐瑞遥 / Candice
- **概念**：三个主题页面，**同一套"皮肤"**（视觉一致），只在**主强调色**和**背景意象**上区分：
  | 页面 | 文件 | 意象 | 强调色 |
  |---|---|---|---|
  | 主页 | `index.html` | **海 Sea** | 海青 `#2E8BA0` |
  | 工作 | `work.html` | **山 Mountain** | 雾岩青 `#4A6B73` |
  | 生活 | `life.html` | **原 Plains** | 草绿 `#7E9B5B` + 麦金 `#E0A94A` |
- **入口**：主页是"个人名片 + 两颗气泡"，点气泡（工作/生活）进入对应页。
- **本人真实信息**（用于填充占位）：
  - 同济大学 · 供应链管理 · **学术型研究生**（研一，2025–2028 在读）
  - 2003.9 生 · 常驻**上海** · 邮箱 `try_candice@163.com`

---

## 2. 设计系统（`css/common.css` 的 `:root`）

**颜色 token（全站共用）**
- `--bg #F8FAFB`、`--surface #FFFFFF`、`--ink #1B2A33`、`--ink-2 #5A6B73`、`--ink-3 #9AA7AD`
- `--line`、`--shadow`
- **`--cream #ECE6D8`** ← 全站统一的"次要文字"米白色（重要，见下方文字规范）

**强调色**：定义在 `body.page-home / .page-work / .page-life` 上（`--accent` / `--accent-soft` / 生活页还有 `--accent-warm`）。

**间距** `--s-1…--s-32`（8 基准）｜**圆角** `--r-sm/--r/--r-lg/--r-pill`｜**动效** `--ease`(慢入快出)、`--ease-soft`、`--t-fast/--t/--t-slow`

**字体**（Google Fonts，每页 `<head>` 都引）
- `--font-display`：`Fraunces` + `Noto Serif SC`（衬线标题）
- `--font-body`：`Hanken Grotesk` + `Noto Sans SC`（无衬线正文）
- 手写/粉笔：`Permanent Marker`、`Caveat Brush`（用于首页 "Welcome~"）
- 兼容别名：`--font-sans`/`--font-serif`（旧代码用，勿删）

### 文字配色规范（用户硬性要求 ⚠️）
**全站文字只用 3 种颜色**，避免"乱七八糟啥都有"：
1. **纯白 `#fff`** —— 主文字 / 标题
2. **米白 `--cream` (#ECE6D8)** —— 次要文字（kicker、说明、占位、页脚、导航轴未高亮）
3. **当页强调色** —— 仅少量点缀（链接 hover、标签、时间、轴刻度高亮等）

**关于阴影**：用户**不喜欢难看的阴影**。需要对比时优先用**颜色 / 字重 / 毛玻璃药丸底**，而不是 text-shadow。
- 已去阴影：右上换页（改毛玻璃药丸）、工作页 kicker。
- 仍保留**极淡**柔影的：直接压在"清晰照片"上的少量文字（Hero 大标题、页脚、卡片整体）——因为没有底衬会糊。卡片内文字靠**玻璃底**衬托。

### 玻璃材质
- 首页名片：偏暗的青玻璃 `rgba(33,58,70,.30)` + 白字。
- 工作页卡片：**带青灰的半透明白雾** `rgba(150,172,182,.32)→…` + 白字/米白 + 一道淡柔影（用户选的方案 A）。
- 引导/药丸：更通透的玻璃。**毛玻璃不要太"毛"**（blur 控制在 10px 上下）。

### 双语 i18n（零构建）
- 每段中英各写一份：`<span lang="zh">…</span><span lang="en">…</span>`
- `html[data-lang]` 控制显隐；`toggleLang()` 切换并存 `localStorage`。
- `js/main.js` **顶部 IIFE** 最先设置 `data-lang` 防闪烁。

---

## 3. 各页进度

### ✅ 主页 `index.html`（海）—— 基本完成，已多轮打磨
- 背景：`assets/hero_bg.jpg`（**横版海景**，固定、`object-fit:cover`、**不旋转**、渐变兜底）。
- 欢迎语 **"Welcome~"**：`Permanent Marker` + **`#chalk-rough` 蚀刻质感**（干粉笔"擦色"，非扭曲）、**逐字书写**、写完**自绘小太阳**（SVG 描边动画 + 抖动）。纯白、`left:32% top:3vh`、~156px。
- 人像 `assets/person.png` 偏左（`left:9% height:84vh bottom:2.5vh`）。
- **右侧一坨**（`.right-col`，整组 `scale(1.1)`、`width:min(50vw,540px)`、紧凑）：横向名片（暗青玻璃白字，📍上海在✉邮箱前、同一行）→ 引导玻璃面板 → **两颗仿真气泡**（工作/生活，点击**破裂迸溅 32 个小气泡**后进入）。
- 顶部粉笔小控件（`EN/中` + 音乐 `♪`）、飘落星星、三层鼠标视差（`data-depth`：bg .04 / person .10 / card .18）+ 名片朝光标 3D 转身。
- 底部居中版权页脚。

### 🚧 工作页 `work.html`（山）—— 结构完成，细节打磨中
- **整屏翻页 deck**：`scroll-snap y mandatory`，**8 屏**：Hero(含简介) → 教育 → 项目 → 工作/实习 → 技能 → 成果/获奖 → 简历 → 联系。
- 背景：`assets/hero-mountain.png` **固定清晰**、**无蒙版**、**滚动不缩放**、**隐藏滚动条**（修掉了右侧黑条）。
- Hero：衬线大标题 "工作·学术"（~99px）、kicker 米白无阴影、名字、简介。
- 卡片：青灰白雾玻璃 + 白字/米白；进入视口**弹入**（`reveal.in`，deck **每次进入重弹**）。
- 指引：**粗箭头**（SVG chevron）。Hero 带文字"点击进入"，其余各屏**只箭头**（JS 生成）；点击 → **弹性滚动**到下一屏（`goTo()`，easeOutBack 回弹）。
- 左侧 **章节导航轴**：无序号，刻度 + 名称浮出 + 高亮 + 点击跳转。
- 左上角粉笔控件；右上角换页（**毛玻璃药丸 + 纯白加粗**，← 首页 / 生活 →）。
- 飘落**绿叶**（**已去阴影**）。

### ✅ 生活页 `life.html`（原）—— 已按新皮肤重做（2026-06-04，与工作页同款 deck）
- **整屏翻页 deck**（与工作页**同一套机制**）：`scroll-snap y mandatory`，**6 屏**：Hero → 性格 → 爱好 → 宠物 → 美食榜 → 交友。
- 背景：`assets/hero_field.png` **固定清晰·无蒙版·不缩放**；飘落**花瓣**（🌸🌼🌾🍃，复用工作页飘叶机制）。
- 皮肤：暖调白雾玻璃卡（白字 + 淡柔影）、**草绿 `--accent`/麦金 `--accent-warm` 强调色**、更圆润的 **Baloo 2** 标题字；左上粉笔控件、右上换页药丸（← 首页 / 工作 →）、左侧章节轴、底部居中页脚——**与首页/工作页一致**。
- 彩蛋互动（`docs/03`）：① ENFJ 名牌**点击翻面** ② 特质条**进入视口充能**（green→wheat） ③ 爱好神秘卡**悬停揭晓** ④ 宠物**「戳我」喷爱心** ⑤ 美食 No.1**点击撒花**。均在 `prefers-reduced-motion` 下关闭。
- 占位：宠物主角/照片墙暂用**玻璃 emoji 占位**（非 broken img），待真图换 `<img>`（HTML 内有 TODO 注释）。
- 验收（Codex Preview 1536×864 量化）：箭头距版权 ~22px 不压；各卡不压箭头（宠物卡已从 555→448px，原 4:5 占位改 1:1）；deck 回弹 overshoot ~84px 后精确停靠。

---

## 4. 文件结构

```
Myself/
├── index.html / work.html / life.html
├── AGENTS.md（本文件）/ README.md
├── css/  common.css(地基) · home.css · work.css · life.css
├── js/   main.js（全站唯一脚本，按编号分段）
├── tools/ make_assets.py（一键抠图+生成背景，需 rembg/opencv/Pillow）
├── docs/ 00 设计系统总纲 · 01 主页·海 · 02 工作·山 · 03 生活·原
└── assets/ 图片素材
```

**`js/main.js` 分段**：① 语言初始化 IIFE + `toggleLang` ② 页面交互：2.1 滚动揭示`.in` / 2.2 年份 / 2.3 锚点高亮 / 2.4 首页三层视差(+#card 3D转身) / 2.5 星星 / 2.6 气泡破裂 / 2.7 欢迎语逐字书写+小太阳 / 2.8 音乐播放开关 / 2.9 飘落（`.leaves`飘叶 ‖ `.petals`飘花瓣，同机制按容器切换）/ 2.10 **deck（工作 + 生活页通用**：选择器认 `.w-*`/`.l-*`，snap + `goTo` 弹性滚动 + 生成箭头指引 + 重弹观察器；`springTo` 动画期间强制 `scroll-behavior:auto` 防与 CSS smooth 打架）/ 2.11 生活页彩蛋（ENFJ 翻面 / 戳我喷爱心 / No.1 撒花）。
> ⚠️ deck/飘落逻辑**两页共用**，改 `.w-*` 时记得 `.l-*` 也受影响（反之亦然）。

---

## 5. 资源（`assets/`）状态

| 文件 | 用途 | 状态 |
|---|---|---|
| `hero_bg.jpg` | 主页海景背景（横版） | ✅ 已就位 |
| `hero-mountain.png` | 工作页山景背景 | ✅ 已就位 |
| `hero_field.png` | 生活页田野背景 | ✅ 已就位（生活页已启用；注意是**竖图** 1080×1799，cover 裁切） |
| `person.png` | 主页人像抠图 | ✅ |
| `hero_sea.png` / `hero_sea_orig.png` | 旧主页竖图（已弃用，留档） | 可清理 |
| `avatar.jpg` | 名片头像（现用"唐"字占位） | ⬜ 待提供 |
| `wechat-qr.jpg` | 微信二维码（悬停弹出） | ⬜ 待提供 |
| `music.mp3` | 背景音乐（♪ 播放） | ⬜ 待提供 |
| `resume.pdf` | 简历下载 | ⬜ 待提供 |

---

## 6. 用户偏好 & 工作约定（重要，先读这条）

- 🖥️ **屏幕 1536px 宽，所有布局按 1536 设计**（首屏一眼的构图以此为准）。
- 🌐 **中英双语**全站保留。
- 🎨 **三页视觉一致**（同皮肤），只换强调色与背景意象。
- 🅰️ **文字仅 3 色**（白 / 米白 `--cream` / 当页强调色），**少用阴影**，要对比优先用颜色·字重·毛玻璃药丸。
- 🪟 偏好**玻璃质感**（适当透明、别太"毛"）；背景照片要**清晰**、**不要丑蒙版**。
- 🧭 **动手前先澄清**：用户喜欢"**一次只问一个问题**、层层追问到 95% 把握再给方案"的流程——遇到方向性决策，先问，别擅自发挥。
- ✅ 每次改完用 **Codex Preview** 自检（见下），**用 `getBoundingClientRect`/`getComputedStyle` 量化验证**，截图作辅助。

---

## 7. 本地预览

- 服务：`python -m http.server 5500`，配置在 **`D:\vibe\try1\.Codex\launch.json`**（name=`myself`），`--directory` 指向 `D:/vibe/Myself`。
- ⚠️ **路径注意**：网站在 `D:\vibe\Myself`，但 harness 工作目录是 `D:\vibe\try1`，两者不同。
- 预览技巧：CSS 改动后 `<head>` 的 `css/*.css` 不带 cache-bust，需在 eval 里给 `<link>` 追加 `?v=` 强刷；导航用 `?v=时间戳`。
- 已知环境问题：大量 `backdrop-filter` + 无限动画时**截图偶尔超时/卡**，**重启 preview server** 可恢复；高分屏截图会被缩小（量化检查更可靠）。

---

## 8. 待办 / TODO

**内容（等用户补真实资料）**
- [ ] 工作页占位块：简介、项目、工作/实习、技能、成果/获奖（教育的本科信息也待补）
- [ ] 生活页占位：ENFJ 三条特质比例 + 名牌背面那句、爱好（含神秘卡彩蛋）、宠物名字+照片(`pet.jpg`)、照片墙(`life-1/2/3.jpg`)、美食榜（No.1 神评语）
- [ ] LinkedIn 链接、`avatar.jpg`、`wechat-qr.jpg`、`music.mp3`、`resume.pdf`

**开发**
- [x] **生活页 `life.html`（原）整体重做**——已按新皮肤（deck）完成（2026-06-04），详见 §3。
- [ ] **移除首页残留的 `.hero-veil`**（`css/home.css`，用户说过"首页不用蒙版"，目前是一层极淡白渐变）
- [x] 工作页验收（2026-06-04，Codex Preview 1536×864 量化）：
  - **粗箭头压版权 → 已修**：`.scroll-cue` bottom `--s-12`→`--s-16`，箭头距版权由 ~6px 提到 **~22px**（hero + 各屏一致），卡片距箭头仍 ≥103px 不碰。
  - **白雾卡白字可读性 → 验收通过**：背景照实测亮处亮度高（avg .35–.58，雪/天空尖峰 ~.99），纯亮度对比偏低，但靠**白字深色柔影**逐字撑起可读性；卡片左对齐文字落在较暗山体上、亮斑在右侧少字区。此为用户选定的**方案 A**，未擅改。
  - **deck 回弹手感 → 验收通过**：点指引 0→864，越过到 949（overshoot ~85px）再弹回精确停在 864。
  - 小观察：左上粉笔控件(高24)与右上药丸(高34)同 `top:20` 顶对齐、左右对称，但垂直中心差 ~5px（并入下方"三页控件统一"一并处理，本次未动）。
- [ ] 把"3 色文字 + 少阴影"规范**回填到首页**，确保三页一致
- [ ] 三页控件（语言/音乐/换页/页脚）样式与位置统一对齐

**可选清理**
- [ ] 删除弃用的 `assets/hero_sea*.png`
- [ ] `css/common.css` 里 `--font-sans/--font-serif` 兼容别名，待各页全部重做后可移除

---

*最后更新：2026-06-04 ① 完成工作页验收；② **生活页 `life.html` 按 deck 新皮肤整体重做完成**（三页皮肤/机制现已统一）。下一步建议：把"3 色文字 + 少阴影"规范回填首页、移除首页 `.hero-veil`、统一三页控件，再等用户补真实资料。*
